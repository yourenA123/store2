<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helper.store.dao.WantBuyMapper">


  <update id="saveShoe" parameterType="com.helper.store.domain.WantBuy">
    update want_buy set is_sell = 1 where id = #{id}
  </update>

  <select id="getUserWantBuy" parameterType="map" resultType="map">
    select want_buy.*,shoe.*
    from want_buy,shoe
    where create_user=#{userId} and want_buy.shoe_id=shoe.id

  </select>

</mapper>